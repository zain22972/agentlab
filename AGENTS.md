# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub issues in [`zain22972/agentlab`](https://github.com/zain22972/agentlab), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
